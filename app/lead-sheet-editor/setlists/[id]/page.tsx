import type { Metadata } from "next";
import SetlistDetail from "./page-client";

export const metadata: Metadata = {
  title: "Setlist - Lead Sheet Editor",
  description: "View and run through a setlist of lead sheets.",
};

export default SetlistDetail;
