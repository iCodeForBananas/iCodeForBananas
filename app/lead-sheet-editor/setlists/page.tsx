import type { Metadata } from "next";
import SetlistList from "./page-client";

export const metadata: Metadata = {
  title: "Setlists - Lead Sheet Editor",
  description: "Organize your lead sheets into setlists and run through them in performance mode.",
};

export default SetlistList;
