import type { Metadata } from "next";
import DrmClient from "./page-client";

export const metadata: Metadata = {
  title: "DRM | iCodeForBananas",
  description: "Your personal ecosystem — track people, evidence, and pillars.",
};

export default DrmClient;
