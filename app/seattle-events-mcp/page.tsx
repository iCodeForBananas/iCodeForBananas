import type { Metadata } from "next";
import SeattleEventsMcpPage from "./page-client";

export const metadata: Metadata = {
  title: "Seattle Events MCP",
  description: "Browse upcoming Seattle events served by the Seattle Events MCP tool.",
};

export default SeattleEventsMcpPage;
